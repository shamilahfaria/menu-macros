{
  "name": "menu-macros",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "node --test tests/**/*.test.js",
    "check:packs": "node scripts/check-pack-freshness.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "esbuild": "^0.28.2",
    "jsdom": "27.0.1"
  }
}
